interface:
  display_name: "Pns Server"
  short_description: "Repo-specific patterns and workflows for pns-server"
  default_prompt: "Use the pns-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true